<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Logged_in_BD__c</fullName>
    <description>Allow the BD to see only the Partnership Objected that they are assigned to. This is a hidden field only used for reports and dashboards.</description>
    <externalId>false</externalId>
    <formula>IF( Account__r.Business_Development_Owner__r.Id = $User.Id, TRUE, FALSE)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Logged in BD</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
